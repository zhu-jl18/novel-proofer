# Copy this file to `.env` and fill in values.
# NOTE: `.env` is gitignored; do NOT commit secrets.
#
# This file is used by the UI "保存默认" feature to prefill LLM fields on page load.
# The server reads/writes these keys as plain dotenv (KEY=VALUE).

# OpenAI-compatible endpoint config
# If your endpoint needs a `/v1` prefix, include it in base_url (e.g. https://api.openai.com/v1).
NOVEL_PROOFER_LLM_BASE_URL=https://your-openai-compatible.example
NOVEL_PROOFER_LLM_MODEL=your-model-name
# API key is required for OpenAI official endpoints; may be empty for local gateways.
NOVEL_PROOFER_LLM_API_KEY=your-api-key

# Optional tuning
NOVEL_PROOFER_LLM_TEMPERATURE=0
NOVEL_PROOFER_LLM_TIMEOUT_SECONDS=180
NOVEL_PROOFER_LLM_MAX_CONCURRENCY=20

# Optional JSON passthrough (must be a JSON object). Example:
# NOVEL_PROOFER_LLM_EXTRA_PARAMS={"max_tokens":4096}
NOVEL_PROOFER_LLM_EXTRA_PARAMS=
